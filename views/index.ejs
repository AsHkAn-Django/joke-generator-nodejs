<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Joke Generator</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- Bootstrap CSS CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Rubik&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Rubik', sans-serif;
      background: #f8f9fa;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }
    .joke-container {
      background: white;
      padding: 2rem;
      border-radius: 15px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.1);
      max-width: 600px;
      width: 100%;
    }
    .joke-text {
      font-size: 1.2rem;
      margin-bottom: 1rem;
    }
  </style>
</head>
<body>

  <div class="joke-container">
    <h1 class="text-center mb-4">😂 Random Joke Generator</h1>

    <% if (joke) { %>
      <div class="joke-text text-center"><%= joke %></div>
    <% } else { %>
      <div class="text-danger text-center">No joke available right now.</div>
    <% } %>

    <form method="GET" action="/" class="mt-4">
      <div class="mb-3">
        <label for="category" class="form-label">Choose a category:</label>
        <select class="form-select" name="category" id="category">
          <option value="Any">Any</option>
          <option value="Programming">Programming</option>
          <option value="Dark">Dark</option>
          <option value="Pun">Pun</option>
          <option value="Spooky">Spooky</option>
          <option value="Christmas">Christmas</option>
        </select>
      </div>
      <div class="d-grid">
        <button type="submit" class="btn btn-primary">Get Joke</button>
      </div>
    </form>
  </div>

</body>
</html>
